# Documentation app

Fumadocs guides for local setup, architecture, authentication, MCP, and agent-client connections.

```bash
bun run dev:docs
```

Edit the shared pages under [`../../docs`](../../docs/README.md). Search, generated Markdown routes, and Open Graph images are included.
