# Documentation app

Fumadocs guides for local setup, architecture, authentication, MCP, and agent-client connections.

```bash
bun run dev:docs
```

Edit pages under `content/docs`. Search, generated Markdown routes, and Open Graph images are included.
